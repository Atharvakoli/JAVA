package hashtableandhandamap;

public class HashtableAndHashMap {
    public static void main(String[] args) {
        // What is hash table
        // how does hash table work
        // equals() and hashCode()
        // hashmap
        // initial capacity and load factor
        // practices with hashmap
        // Immutable keys and how to lost value in map
        // sort Map by keys
        // sort map by values

        // Requirements for hashcode() method
        // Multiple invocation of hashcode methods must consistently return the same
        // integer
        // If two objects are equal, then their hash codes also should be equal
        // In case two objects are not equal, it is possible that hash codes will be the
        // same

        // Requirements for equals method
        // equals methods should be reflexive x.equals(x) == true
        // equals method should symmetric x.equals(y) == y.equals(x)
        // transtive x.equals(y) == y.equals(z) == x.equals(z)
        // consistent
        // if x != null, then x.equals(null) == false
    }
}
