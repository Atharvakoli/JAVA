package linkedhashmap;

public class LinkedHashMap {
    public static void main(String[] args) {
        // LinkedHashMap
        // access-order vs insertion-order in LinkedHashMap
        // What is a cache - Cache is a hardware or software component that stores data
        // so that future requests for that data can be served faster. The data stored
        // in the cache might be as a result of an earlier computation or a copy of data
        // stored elsewhere
        // Cache replacement policies -> FIFO, LIFO, Least recently Used (LRU), Most
        // recently user (MRU), Random replacement, lease frequently user (LFU), LFRU
        // How to limit number of elements in LinkedHashMap
        // Homework LRU Cache
    }
}
